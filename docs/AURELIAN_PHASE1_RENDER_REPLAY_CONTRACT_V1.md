# Aurelian Phase 1 Exact-GLB Product-Camera Render Replay Contract v1

Status: ACCEPTED FOR ONE BOUNDED EVIDENCE REPLAY
Issue: #415
Source candidate: rejected PR #429 exact head `be801c6aad0ea836210ef85929d8bc95c3152525`
Source artifact: `9222116061`
Source artifact digest: `sha256:d810b371584d1da404b1c34e64270c5d38158c74a7d775560b16fa2228cb6990`
Source GLB SHA256: `2014fadc94cbc6a53f30c097788036775c878b6e6540d30d108ddb40c8b903e7`
Diagnostic proof: PR #435 exact head `34c05e857b7c7e36e796025e6d104eaf98d96ba7`, run `32030900002`, artifact `9288859851`

## Purpose

Reveal the first truthful Village / Map / World / Bridge product-camera evidence of the exact #429 GLB before spending on another Aurelian art implementation.

PR #435 proved the pinned GLB is non-empty and renderable with original materials in Godot 4.7.1 Compatibility in both main viewport and an actively consumed 1440×900 SubViewport. It also isolated the #429 black-output cause to its environment configuration: `environment.tonemap_exposure = 0.0`.

This replay is evidence-only. It is not a new Basin candidate, an art correction pass or product integration.

## Exact input and invariants

The replay must:

1. download and verify artifact `9222116061`;
2. verify artifact digest and internal GLB SHA above;
3. use the GLB unchanged, without Blender, KayKit regeneration, geometry edits, material replacement or topology edits;
4. use Godot 4.7.1 Compatibility;
5. preserve the intended #429 shared-scene camera transforms and framing;
6. use normal exposure, `tonemap_exposure = 1.0` or the Godot default, never `0.0`;
7. capture from one imported scene instance family, never independently authored geographies.

## Required camera contract

Use the exact #429 topology-camera definitions:

- Village: center `[425,315]`, orthographic size `52.0`;
- Map: center `[500,435]`, orthographic size `103.0`;
- World: center `[500,465]`, orthographic size `132.0`;
- Bridge: center `[515,340]`, orthographic size `27.0`.

Use the same topology conversion and camera family as #429:

- topology scale `0.12`;
- topology center `[500,450]`;
- camera position = focus + `[70,88,95]`;
- near `0.1`;
- far `1200.0`;
- camera looks at its focus with `Vector3.UP`.

No framing improvement is allowed before the first exact replay evidence exists.

## Environment contract

Preserve the #429 environment closely enough to judge its intended visual composition, with one required correction:

- background `#33494b`;
- ambient color `#c8c5ae`;
- ambient energy `0.54`;
- Filmic tonemapper;
- `tonemap_exposure = 1.0` or default;
- fog color `#a9b2a4`;
- fog density `0.0024`;
- LateMorningSun and CoolFill transforms/colors/energies from #429.

The replay may add a tiny unmistakable corner/sentinel control outside the Basin framing only if needed to prove capture continuity, but it must not obscure or visually alter the scene.

## Allowed scope

- `game/scenes/calibration/aurelian_replay/**`
- one narrow `.github/workflows/aurelian-render-replay.yml`
- evidence manifests and QA JSON generated by the workflow

## Forbidden

- Blender authoring or regeneration;
- KayKit regeneration;
- GLB or source asset edits;
- terrain, river, bridge, road, building or material correction;
- new assets or art direction implementation;
- edits to `app/play/**`;
- gameplay, reducer, persistence, P12, onboarding or retention changes;
- product integration or Phase 2;
- independent Village / Map / World geographies;
- paid tools/assets, MAX, Fable or image generation;
- merging the evidence implementation into product code.

Target extra spend: 0 USD.

## Required deterministic evidence

- exact replay head SHA;
- source artifact ID, digest, source head and GLB SHA;
- Godot version and renderer/adapter;
- import log and foundation-test result;
- exact camera manifest for all four views;
- environment manifest proving exposure `1.0` or default and the intended #429 environment values;
- four exact 1440×900 PNGs: Village, Map, World, Bridge;
- one 15 to 20 second camera-switch movie showing Village → Map → World, with Bridge represented by still;
- image hashes, dimensions, variance/color stats and pairwise deltas;
- direct review of all four stills and representative movie frames.

A green workflow alone is never acceptance.

## Direct visual review questions

The review must judge the exact existing scene, not excuse it as a diagnostic prototype:

1. Does Village read as a believable settlement area with a physically integrated crossing?
2. Does the bridge visibly connect west and east dry-ground approaches instead of floating or reading as an isolated prop?
3. Does Map make the required road and river network legible?
4. Does World read as one coherent Aurelian Basin with a continuous river and integrated southern outflow?
5. Are Greenvale, North Ridge, forest/work edge, fields/plains, marsh and coast spatially understandable?
6. Do all cameras clearly show the same geography and useful scale progression?
7. Is the visual baseline materially strong enough to justify salvaging this exact GLB rather than reauthoring the scene?

## Classification

Choose exactly one after direct evidence review:

- `VISUAL_REPLAY_PASS_FOR_SALVAGE`: the exact GLB is materially credible enough to justify a later bounded salvage contract. This is not product acceptance or integration authority.
- `VISUAL_REPLAY_REJECT`: truthful product-camera evidence confirms the exact GLB is not a suitable visual baseline. Stop patching it and move to a fresh art-direction/recovery contract.
- `INVALID`: provenance, exact input, camera/environment contract, dimensions, movie or required direct evidence is incomplete.

## Stop condition

Stop after one exact-head replay artifact receives direct review and one classification above.

The evidence PR must close without merge. Then update `docs/PROJECT_CURRENT_STATE.md` through a separate normal control-plane PR before any further Aurelian implementation.

If `VISUAL_REPLAY_REJECT`, do not attempt another code correction to this GLB. The next allowed work is a fresh art-direction/recovery contract.

If `VISUAL_REPLAY_PASS_FOR_SALVAGE`, the next allowed work is a separate bounded salvage contract. Do not integrate directly.